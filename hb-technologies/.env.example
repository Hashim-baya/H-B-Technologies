# Frontend public config
# Use the final production origin in deployed environments.
# Production builds intentionally fail if this resolves to localhost.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only (used by Next.js route handlers to call the Express API)
API_URL=https://h-b-technologies.onrender.com
